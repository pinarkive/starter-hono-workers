# Workers do not load .env at runtime.
# Local: copy .dev.vars.example → .dev.vars and set PINARKIVE_API_KEY.
# Production: wrangler secret put PINARKIVE_API_KEY
#
# PINARKIVE_API_BASE_URL and optional PINARKIVE_CLUSTER_ID: see wrangler.jsonc / dashboard.
